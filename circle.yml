machine:
  ruby:
    version: ruby-2.3.4
  xcode:
    version: 9.0.1
dependencies:
  pre:
    - brew update
    - brew upgrade
    - brew install node 8.6.0
    - brew switch node 8.6.0
compile:
  override:
    - ./deploy.sh